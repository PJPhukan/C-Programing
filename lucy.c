#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    int arr[100], n, i, l;

    // int a,b,n;
    printf("Enter How many number in the series :");
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        scanf("%d", &arr[i]);
    }
    printf("Enter your lucy number");
    scanf("%d", &l);
    for(i=0;i<n;i++){
        if(arr[i]<)
    }
    printf("%d",i);
    return 0;
}
